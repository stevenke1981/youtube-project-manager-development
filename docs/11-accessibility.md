# Accessibility

- WCAG 2.2 AA 為目標。
- 所有操作可鍵盤完成。
- 清楚 heading hierarchy 與 landmarks。
- 狀態更新使用適當 live region，不連續播報掃描每個檔案。
- 拖放必須有「選擇檔案」替代流程。
- 表格支援 row focus、sort state、可讀欄位名稱。
- waveform、progress chart 同時提供數值文字。
- 動畫遵循 `prefers-reduced-motion`。
- 不強制使用專案附帶字型；預設 system＋Noto Sans TC fallback。
