# Roadmap

## v0.1 Project Foundation

Library、Project、folder templates、validation、CLI、desktop shell。

## v0.2 Asset Manager

掃描、preview、metadata、versions、archive、relink。

## v0.3 Production Workflow

Tasks、Kanban、deliverables、templates、calendar。

## v0.4 Script & Subtitle

Markdown scene editor、autosave、SRT/VTT/ASS、validation。

## v0.5 Media Tools

FFprobe、thumbnails、waveform、audio normalization、proxy。

## v0.6 Local AI

Whisper.cpp、Qwen3-TTS／VoxCPM adapters、job queue。

## v0.7 Publishing

YouTube OAuth、upload draft、thumbnail、subtitle、schedule。

## v0.8 Media Workstation — completed

Portable non-destructive NLE timeline、FFprobe inspector、FFmpeg preview/proxy/final export、progress/cancel 與可重建 render manifest。

## v0.9 Safe Publishing — completed

YouTube OAuth loopback + PKCE、metadata/thumbnail/subtitle upload、dry-run、explicit confirmation、retry/cancel 與 credential reference。

## v1.0 NLE Production — current

Timeline schema v2 migration、typed professional effects、SRT/VTT→ASS subtitle burn-in、完整 multi-track filter graph、background export queue、進度／取消、numbered editor workflow、MSI/NSIS 與 release smoke。

## v1.1 Stable

Signed installers、clean install/upgrade/uninstall-preserves-library tests、thumbnail/proxy cache、waveform、hardware encoder profiles、versions/history、performance target 與完整使用手冊。
